---
phase: 01-foundation-orchestration
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: ["web/server.js", "api/Procfile", "Procfile"]
autonomous: true
requirements: ["MONO-02", "MONO-04"]
gap_closure: true

must_haves:
  truths:
    - "Express server starts up correctly without crashing on wildcard syntax errors"
    - "Procfile resides in the root directory and correctly references both sub-services"
  artifacts:
    - path: "web/server.js"
      provides: "Express API and SPA routing"
      contains: "app.get("
    - path: "Procfile"
      provides: "Process manager orchestration"
      contains: "web: node web/server.js"
  key_links:
    - from: "Procfile"
      to: "web/server.js"
      via: "command"
      pattern: "node web/server.js"
---

<objective>
Fix Express v5 routing syntax error and relocate Procfile to root.

Purpose: Address verification gaps where `web/server.js` crashes due to unsupported Express 5 wildcard syntax, and `Procfile` is in the wrong directory preventing Foreman from starting the application correctly.
Output: Working Express server and correctly placed root Procfile.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-orchestration/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Express wildcard routing syntax</name>
  <files>web/server.js</files>
  <action>
    - Update the fallback route in `web/server.js`.
    - Replace `app.get('*', (req, res) => ...)` with `app.get(/(.*)/, (req, res) => ...)` or `app.get('*splat', ...)`. Express 5 updated `path-to-regexp` and no longer supports `*` on its own.
    - Reference existing code: The Express server is a BFF that serves static assets and falls back to index.html.
    - Gap reason: Server crashes immediately on startup due to Express 5 router wildcard syntax error.
  </action>
  <verify>
    <automated>node web/server.js</automated>
    Note: Provide dummy env variables if needed, or simply verify the process starts without instantly throwing `PathError`. If it blocks forever, run it with timeout via bash to just confirm successful init. For example: `powershell -Command "Start-Process node web/server.js -PassThru | Wait-Process -Timeout 2; if ($?) { exit 0 }"`
  </verify>
  <done>Express server starts without throwing a PathError.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate Procfile to repository root</name>
  <files>api/Procfile, Procfile</files>
  <action>
    - Move `api/Procfile` to the root directory `Procfile`.
    - Ensure the commands inside remain `web: node web/server.js` and `api: cd api && uvicorn app.main:app --host 0.0.0.0 --port 8000` (they were originally written for root execution).
    - Delete the `api/Procfile` file to avoid confusion.
    - Gap reason: `Procfile` was created in `api/` directory by mistake, breaking Foreman orchestration.
  </action>
  <verify>
    <automated>cat Procfile | Select-String "web: node web/server.js"</automated>
  </verify>
  <done>Procfile exists in root directory and api/Procfile is gone.</done>
</task>

</tasks>

<verification>
- `node web/server.js` does not instantly crash.
- `Procfile` exists in the root of the workspace.
</verification>

<success_criteria>
- Both identified gaps from VERIFICATION.md are closed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-orchestration/01-03-SUMMARY.md`
</output>
