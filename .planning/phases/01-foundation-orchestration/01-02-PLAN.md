---
phase: 01-foundation-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - api/Procfile
autonomous: false
requirements:
  - MONO-04
must_haves:
  truths:
    - "Single command starts both FastAPI and Express servers"
    - "Both processes terminate cleanly when stopped"
    - "React app proxy routes successfully to Python backend"
  artifacts:
    - path: "package.json"
      provides: "concurrent startup scripts"
      min_lines: 5
    - path: "api/Procfile"
      provides: "foreman compatibility"
      exports: []
  key_links:
    - from: "package.json scripts"
      to: "api and web processes"
      via: "concurrently"
      pattern: "concurrently"
---

<objective>
Setup the unified monorepo orchestration using `concurrently` in the root `package.json` to boot both the React frontend and Python backend together.

Purpose: Enable developers to start the entire Forex dashboard ecosystem with one command, ensuring simplified workflow.
Output: A functional `package.json` with multi-service runner scripts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Root Task Runner</name>
  <files>package.json</files>
  <action>
    Create a root `package.json` acting purely as a task runner.
    1. Initialize with basic metadata (name: `forex-predictor-monorepo`, private: true).
    2. Add `concurrently` and `npm-run-all` as `devDependencies`.
    3. Define the following scripts:
       - `install:all`: Runs `npm install` in `web/` and ensures Python deps are present.
       - `dev:api`: Runs the FastAPI backend (`uvicorn app.main:app --app-dir api --reload --port 8000`).
       - `dev:web`: Runs the Vite dev server (`npm run dev --prefix web`).
       - `dev`: Uses `concurrently` to run both `dev:api` and `dev:web` simultaneously.
       - `start`: Runs the production start (Express server and API).
  </action>
  <verify>
    <automated>npm install && cat package.json | grep concurrently</automated>
  </verify>
  <done>Root runner is configured</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint Verify</name>
  <files>N/A</files>
  <action>Human verification step for startup and routing.</action>
  <what-built>Unified start command and proxy routing</what-built>
  <how-to-verify>
    1. Run `npm run dev` from the project root.
    2. Open the React app in your browser (typically `http://localhost:5173`).
    3. Confirm the React app loads successfully.
    4. Confirm that any requests mapped to `/api` (or `/api/v1/health` specifically) are routed to the Python backend on `8000`.
    5. Press `Ctrl+C` in the terminal and verify both Node and Python processes terminate completely.
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification"</automated>
  </verify>
  <done>User confirms everything works correctly</done>
  <resume-signal>Type "approved" or describe any routing/termination issues</resume-signal>
</task>

</tasks>

<verification>
- Running `npm run dev` successfully launches `api/app.main` and `web/vite`.
</verification>

<success_criteria>
- Both applications boot concurrently using a single command.
- The web frontend correctly proxies data from the backend.
- Clean process shutdown without zombie instances.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-orchestration/01-02-SUMMARY.md`
</output>
