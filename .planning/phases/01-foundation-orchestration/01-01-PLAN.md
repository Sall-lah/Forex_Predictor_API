---
phase: 01-foundation-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/vite.config.ts
  - web/server.js
  - web/src/App.tsx
autonomous: true
requirements:
  - MONO-01
  - MONO-02
  - MONO-03
  - UI-03
must_haves:
  truths:
    - "React app is scaffolded and buildable"
    - "Express server is configured to serve API requests"
    - "Vite dev server correctly proxies /api requests"
  artifacts:
    - path: "web/package.json"
      provides: "web dependencies and scripts"
      min_lines: 10
    - path: "web/server.js"
      provides: "Express BFF server"
      exports: []
  key_links:
    - from: "web/vite.config.ts"
      to: "api/app/main.py"
      via: "proxy configuration"
      pattern: "target.*8000"
---

<objective>
Scaffold the React frontend and Express Backend-For-Frontend (BFF) within the `web/` directory.

Purpose: Provide the foundation for the Forex Dashboard UI, establishing a proxy to bypass CORS and simplify client-side routing.
Output: A functional Vite React scaffold and an Express server proxy.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold React + Vite application</name>
  <files>web/package.json, web/vite.config.ts, web/src/App.tsx</files>
  <action>
    Initialize a standard React + TypeScript Vite project in the `web/` directory. 
    1. Create standard `package.json` for React/Vite.
    2. Add dependencies: `react`, `react-dom`, `express`, `cors`, `http-proxy-middleware`.
    3. Add devDependencies: `vite`, `typescript`, `@types/react`, etc.
    4. Configure `vite.config.ts` to proxy requests starting with `/api` to the backend running at `http://localhost:8000`.
  </action>
  <verify>
    <automated>npm --prefix web install && npm run build --prefix web</automated>
  </verify>
  <done>React project is scaffolded and builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Setup Express BFF Proxy</name>
  <files>web/server.js</files>
  <action>
    Create the Express backend-for-frontend server (`server.js`).
    This server should:
    1. Listen on port 3000.
    2. Serve static files from the `dist/` directory (for production).
    3. Proxy any requests to `/api` over to `http://localhost:8000` (FastAPI backend).
    4. Add a basic `GET /health` endpoint for the BFF itself returning `{"status": "ok", "service": "web-bff"}`.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); if(!fs.existsSync('web/server.js')) process.exit(1);"</automated>
  </verify>
  <done>Express server file is created and correctly proxies API requests</done>
</task>

</tasks>

<verification>
- `web/package.json` contains scripts and dependencies for React and Express.
- `web/server.js` is structured as a proxy.
</verification>

<success_criteria>
- Vite project is scaffolded.
- The web build succeeds (`npm run build --prefix web`).
- Express server proxy logic is established.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-orchestration/01-01-SUMMARY.md`
</output>
