---
phase: 03-documentation-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - docs/rate-limiter-configuration.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "Developer reading AGENTS.md can explain the runtime flow middleware -> service -> bucket -> storage and where to extend behavior safely."
    - "Operator can configure default and per-endpoint limits using exact RATE_LIMIT_* environment variables without opening source code."
    - "Developer/operator can identify exempt endpoints and understand exact exemption matching rules plus trusted-proxy caveats."
  artifacts:
    - path: "AGENTS.md"
      provides: "Project-level developer guidance for rate limiter architecture and extension patterns"
      contains: "Rate Limiter Architecture"
    - path: "docs/rate-limiter-configuration.md"
      provides: "Configuration reference with concrete env var examples and exemption guide"
      contains: "RATE_LIMIT_DEFAULT_CAPACITY"
  key_links:
    - from: "AGENTS.md"
      to: "app/middleware/rate_limit/service.py"
      via: "Architecture section maps responsibilities to concrete classes/files"
      pattern: "RateLimiterService|TokenBucket|InMemoryRateLimitStorage"
    - from: "docs/rate-limiter-configuration.md"
      to: "app/core/config.py"
      via: "Environment variable names and defaults mirror Settings fields"
      pattern: "RATE_LIMIT_"
---

<objective>
Document the implemented rate limiter so future contributors and operators can safely maintain, configure, and extend it without reverse-engineering code.

Purpose: Close all Phase 3 documentation requirements and reduce onboarding/configuration errors.
Output: Updated AGENTS.md plus a dedicated configuration/exemption guide.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documentation-integration/03-RESEARCH.md
@app/core/config.py
@app/middleware/rate_limit/middleware.py
@app/middleware/rate_limit/service.py
@app/middleware/rate_limit/bucket.py
@app/middleware/rate_limit/storage.py
@app/main.py
</context>

<interfaces>
From `app/middleware/rate_limit/service.py`:
```python
class RateLimiterService:
    async def evaluate(self, request: Request) -> RateLimitServiceResult: ...
    def _resolve_client_ip(self, request: Request) -> str: ...
    def _resolve_policy(self, path: str) -> tuple[str, RateLimitPolicy]: ...
```

From `app/middleware/rate_limit/middleware.py`:
```python
class RateLimitMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request: Request, call_next) -> Response: ...
```

From `app/core/config.py`:
```python
RATE_LIMIT_DEFAULT_CAPACITY
RATE_LIMIT_DEFAULT_REFILL_RATE_PER_SECOND
RATE_LIMIT_PREDICTION_CAPACITY
RATE_LIMIT_PREDICTION_REFILL_RATE_PER_SECOND
RATE_LIMIT_HISTORICAL_CAPACITY
RATE_LIMIT_HISTORICAL_REFILL_RATE_PER_SECOND
RATE_LIMIT_STORAGE_MAX_ENTRIES
RATE_LIMIT_STORAGE_TTL_SECONDS
RATE_LIMIT_TRUSTED_PROXY_IPS
RATE_LIMIT_EXEMPT_PATHS
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update AGENTS.md with rate limiter architecture and extension patterns</name>
  <read_first>
    - AGENTS.md
    - app/main.py
    - app/middleware/rate_limit/middleware.py
    - app/middleware/rate_limit/service.py
    - app/middleware/rate_limit/bucket.py
    - app/middleware/rate_limit/storage.py
    - app/core/config.py
  </read_first>
  <files>AGENTS.md</files>
  <action>Add a new `## Rate Limiter Architecture` section in `AGENTS.md` documenting the exact runtime chain `app.main -> RateLimitMiddleware.dispatch() -> RateLimiterService.evaluate() -> TokenBucket.consume() + InMemoryRateLimitStorage`, with one bullet per layer responsibility and explicit "do not move business logic into middleware" guidance. Add a `## Rate Limiter Extension Checklist` section that includes concrete steps: add new `RATE_LIMIT_*` fields in `Settings`, map path prefix in `_resolve_policy`, add/adjust middleware tests in `tests/middleware/rate_limit/test_middleware.py`, and run `pytest tests/middleware/rate_limit/ -x` (implements DOCS-01).</action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('AGENTS.md').read_text(encoding='utf-8'); assert 'Rate Limiter Architecture' in t; assert 'RateLimiterService' in t; assert 'TokenBucket' in t; assert 'InMemoryRateLimitStorage' in t"</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` contains heading `## Rate Limiter Architecture`.
    - `AGENTS.md` contains exact class names `RateLimitMiddleware`, `RateLimiterService`, `TokenBucket`, and `InMemoryRateLimitStorage`.
    - `AGENTS.md` contains heading `## Rate Limiter Extension Checklist`.
  </acceptance_criteria>
  <done>AGENTS.md fully explains architecture and provides concrete extension steps for future developers.</done>
</task>

<task type="auto">
  <name>Task 2: Create operator configuration guide with concrete env var examples</name>
  <read_first>
    - app/core/config.py
    - AGENTS.md
    - .env
  </read_first>
  <files>docs/rate-limiter-configuration.md</files>
  <action>Create `docs/rate-limiter-configuration.md` with sections: `Overview`, `Environment Variables`, `Per-endpoint Overrides`, `Recommended Starter Profiles`, and `Validation Commands`. Include exact variable names from `Settings` with current defaults (`RATE_LIMIT_DEFAULT_CAPACITY=60`, `RATE_LIMIT_PREDICTION_CAPACITY=10`, `RATE_LIMIT_HISTORICAL_CAPACITY=100`, etc.), include a copy-paste `.env` snippet showing stricter prediction and looser historical limits, and include explicit validation commands `pytest tests/middleware/rate_limit/test_middleware.py -x` and `uvicorn app.main:app --reload` (implements DOCS-02).</action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('docs/rate-limiter-configuration.md').read_text(encoding='utf-8'); req=['RATE_LIMIT_DEFAULT_CAPACITY','RATE_LIMIT_PREDICTION_CAPACITY','RATE_LIMIT_HISTORICAL_CAPACITY','RATE_LIMIT_STORAGE_TTL_SECONDS','uvicorn app.main:app --reload']; [(_ for _ in ()).throw(AssertionError(k)) for k in req if k not in t]"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/rate-limiter-configuration.md` exists and contains heading `## Environment Variables`.
    - The guide includes all rate-limit env vars defined in `app/core/config.py`.
    - The guide contains at least one full `.env` example block with concrete numeric values.
  </acceptance_criteria>
  <done>Operators can configure default and per-endpoint limits using the guide alone, without source exploration.</done>
</task>

<task type="auto">
  <name>Task 3: Document exemption configuration and anti-bypass rules</name>
  <read_first>
    - docs/rate-limiter-configuration.md
    - app/middleware/rate_limit/service.py
    - tests/middleware/rate_limit/test_middleware.py
  </read_first>
  <files>docs/rate-limiter-configuration.md, AGENTS.md</files>
  <action>Extend `docs/rate-limiter-configuration.md` with a dedicated `## Exempt Endpoints and Security Notes` section that lists default exempt endpoints exactly (`/health`, `/docs`, `/redoc`, `/openapi.json`), explains path normalization behavior (query string stripped, trailing slash normalized), and states that traversal-like variants are not exempt. Add a short cross-reference block in `AGENTS.md` pointing maintainers to this section and requiring spoofing/exemption regression tests when exemption behavior changes (implements DOCS-03).</action>
  <verify>
    <automated>python -c "from pathlib import Path; d=Path('docs/rate-limiter-configuration.md').read_text(encoding='utf-8'); a=Path('AGENTS.md').read_text(encoding='utf-8'); assert 'Exempt Endpoints and Security Notes' in d; assert '/health' in d and '/docs' in d and '/redoc' in d and '/openapi.json' in d; assert 'query string' in d.lower(); assert 'traversal' in d.lower(); assert 'docs/rate-limiter-configuration.md' in a"</automated>
  </verify>
  <acceptance_criteria>
    - Guide includes explicit defaults for `RATE_LIMIT_EXEMPT_PATHS` values.
    - Guide states anti-bypass behavior for query strings/trailing slashes/traversal-like paths.
    - `AGENTS.md` references `docs/rate-limiter-configuration.md` for exemption behavior.
  </acceptance_criteria>
  <done>Exemption behavior is clearly documented with security caveats, preventing accidental bypass regressions.</done>
</task>

</tasks>

<verification>
Run:
- `python -c "from pathlib import Path; assert Path('AGENTS.md').exists(); assert Path('docs/rate-limiter-configuration.md').exists()"`
- `pytest tests/middleware/rate_limit/test_middleware.py -k "exempt or headers" -x`
</verification>

<success_criteria>
- DOCS-01: AGENTS.md documents architecture flow and extension patterns.
- DOCS-02: Dedicated guide provides concrete env configuration examples and operator commands.
- DOCS-03: Exemption endpoints and anti-bypass rules are explicitly documented and cross-referenced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-integration/03-documentation-integration-01-SUMMARY.md`
</output>
