---
phase: 02-ui-baseline-data-visualization
plan: 03
type: execute
wave: 1
depends_on: ["01", "02"]
files_modified:
  - web/package.json
  - web/tailwind.config.js
  - web/postcss.config.js
  - web/src/index.css
  - web/src/main.tsx
  - web/index.html
autonomous: false
requirements: ["UI-01", "UI-02"]
gap_closure: true

must_haves:
  truths:
    - "Dashboard matches the Stitch template 'Forex Dashboard with SL/TP Controls' visually"
    - "Tailwind utility classes are successfully applied to React components"
    - "Material symbols and fonts load correctly"
  artifacts:
    - path: "web/tailwind.config.js"
      provides: "Tailwind theme configuration"
    - path: "web/src/index.css"
      provides: "Global CSS with Tailwind directives"
  key_links:
    - from: "web/src/main.tsx"
      to: "web/src/index.css"
      via: "CSS import"
      pattern: "import './index.css'"
---

<objective>
Fix the missing CSS styling/classes issue identified during UAT by installing and configuring Tailwind CSS and necessary global assets (fonts/icons).

Purpose: The UI was scaffolded successfully into React, but currently renders as bare HTML because the Tailwind CSS engine and global Styles/Fonts required by the Stitch template were not added to the build pipeline.
Output: A styled dashboard matching the Stitch template's visual appearance.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ui-baseline-data-visualization/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install & Configure Tailwind CSS</name>
  <files>web/package.json, web/tailwind.config.js, web/postcss.config.js, web/src/index.css</files>
  <action>
    Install Tailwind CSS, PostCSS, and Autoprefixer as devDependencies in `web/` (`npm install -D tailwindcss postcss autoprefixer`).
    Create `web/tailwind.config.js` and `web/postcss.config.js` to process CSS.
    In `tailwind.config.js`, configure the `content` array to include `./index.html` and `./src/**/*.{js,ts,jsx,tsx}`.
    Extend the theme to map the Stitch template color classes (e.g., `surface`: '#000000', `secondary`: '#14b8a6', `on-secondary`: '#ffffff', `outline-variant`: '#3f3f46') based on standard dark mode equivalents, and font families (`headline`: ['Inter', 'sans-serif']).
    Create `web/src/index.css` and add the three standard Tailwind directives (`@tailwind base; @tailwind components; @tailwind utilities;`).
    Reference existing code: Gap reason states the classes are present in `Dashboard.tsx` but the engine is missing.
  </action>
  <verify>
    <automated>cd web && npx tailwindcss -i ./src/index.css -o ./src/output.css --minify</automated>
  </verify>
  <done>Tailwind dependencies installed and config files exist.</done>
</task>

<task type="auto">
  <name>Task 2: Inject Global Assets</name>
  <files>web/index.html, web/src/main.tsx</files>
  <action>
    Update `web/index.html` to load required external fonts:
    - Google Fonts for `Inter` (or the font family used in `tailwind.config.js`).
    - Material Symbols Outlined (`<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />`).
    Update `web/src/main.tsx` to `import './index.css'` at the top of the file to ensure Vite processes the Tailwind styles.
  </action>
  <verify>
    <automated>npm run build --prefix web</automated>
  </verify>
  <done>Fonts linked in index.html and index.css imported into main.tsx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual Verification</name>
  <action>
    Ask user to start the frontend server (`npm run dev` in `web/`) and open the application in the browser.
  </action>
  <verify>
    <what-built>Tailwind CSS configuration and global font injection</what-built>
    <how-to-verify>
      Visually confirm the dashboard is no longer unstyled bare HTML. The background should be dark, the header should have a distinct color, layout grids should align elements correctly, and material symbol icons should render.
    </how-to-verify>
    <resume-signal>Type "approved" or describe any visual layout issues.</resume-signal>
  </verify>
  <done>Human verifies styling visually matches the Stitch template.</done>
</task>

</tasks>

<verification>
- `index.css` imported in entry file.
- Tailwind builds classes correctly.
- Human verifies styling visually matches the Stitch template.
</verification>

<success_criteria>
The gap is closed: the application renders correctly styled UI components instead of bare HTML.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-baseline-data-visualization/02-03-SUMMARY.md`
</output>
