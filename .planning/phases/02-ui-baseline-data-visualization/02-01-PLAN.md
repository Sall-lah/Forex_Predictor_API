---
phase: 02-ui-baseline-data-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/src/components/Dashboard.tsx
  - web/src/components/Chart.tsx
  - web/src/components/Layout.tsx
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - User can see a styled dashboard layout based on the Stitch template.
  artifacts:
    - path: web/src/components/Dashboard.tsx
    - path: web/src/components/Chart.tsx
  key_links:
    - from: web/src/App.tsx
      to: web/src/components/Dashboard.tsx
---

<objective>
Scaffold the UI baseline by integrating the Stitch template "Forex Dashboard with SL/TP Controls" and installing `lightweight-charts`.

Purpose: Establish the foundational structure and design system required by the roadmap.
Output: Layout, Dashboard, and empty Chart components using the Stitch UI styling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-baseline-data-visualization/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lightweight-charts</name>
  <files>web/package.json</files>
  <read_first>
    - web/package.json
  </read_first>
  <action>
    Add `lightweight-charts` to `web/package.json` dependencies.
    Run `npm install` inside the `web/` directory.
  </action>
  <verify>
    <automated>cd web && npm ls lightweight-charts</automated>
  </verify>
  <done>lightweight-charts is present in node_modules</done>
  <acceptance_criteria>web/package.json contains lightweight-charts</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Download Stitch Template</name>
  <files>web/src/components/Dashboard.tsx, web/src/components/Chart.tsx</files>
  <read_first>
    - web/src/App.tsx
  </read_first>
  <action>
    Retrieve the React component code for Stitch screen `eadc6ea9218f40febe06ba2f03bc678d` from project `885434743592032491`.
    Save the layout into `web/src/components/Dashboard.tsx`.
    Create an empty chart container in `web/src/components/Chart.tsx` that will house the `lightweight-charts` instance later.
    Integrate `Dashboard` into `web/src/App.tsx`.
  </action>
  <verify>
    <automated>npm run build --prefix web</automated>
  </verify>
  <done>Dashboard renders without errors</done>
  <acceptance_criteria>web/src/components/Dashboard.tsx contains the React component for the dashboard layout</acceptance_criteria>
</task>

</tasks>

<success_criteria>
- Dashboard component exists
- lightweight-charts is installed
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-baseline-data-visualization/02-01-SUMMARY.md`
</output>
