---
phase: 02-ui-baseline-data-visualization
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - web/src/components/Chart.tsx
  - web/src/components/HealthStatus.tsx
  - web/src/hooks/useMarketData.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-05]
must_haves:
  truths:
    - OHLCV candlestick chart displays live historic data
    - Market price distinctively displayed
    - Visual indicator shows backend/Kraken connection health
  artifacts:
    - path: web/src/hooks/useMarketData.ts
      provides: Data fetching hook
    - path: web/src/components/Chart.tsx
      provides: OHLCV charting implementation
  key_links:
    - from: web/src/components/Chart.tsx
      to: web/src/hooks/useMarketData.ts
      via: fetch loop
---

<objective>
Implement live OHLCV data polling, render the candlestick chart, and display connection health status.

Purpose: Provide users with real-time market data visualization and system reliability feedback.
Output: Custom data hook, populated `lightweight-charts` instance, and health indicator.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-baseline-data-visualization/02-CONTEXT.md
@.planning/phases/02-ui-baseline-data-visualization/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Market Data Polling Hook</name>
  <files>web/src/hooks/useMarketData.ts</files>
  <read_first>
    - web/src/components/Dashboard.tsx
  </read_first>
  <action>
    Implement `useMarketData` hook to fetch from `/api/v1/historic-data/live`.
    Set up polling interval per D-02 (2 seconds).
    Return `{ data, error, isHealthy, currentPrice }`.
    `isHealthy` should reflect success or failure (D-03).
  </action>
  <verify>
    <automated>grep useMarketData web/src/hooks/useMarketData.ts</automated>
  </verify>
  <done>Hook successfully implemented with polling logic</done>
  <acceptance_criteria>web/src/hooks/useMarketData.ts exports useMarketData function</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Candlestick Chart Implementation</name>
  <files>web/src/components/Chart.tsx, web/src/components/HealthStatus.tsx, web/src/components/Dashboard.tsx</files>
  <read_first>
    - web/src/components/Chart.tsx
    - web/src/hooks/useMarketData.ts
  </read_first>
  <action>
    Update `Chart.tsx` to initialize `createChart` from `lightweight-charts` and add a candlestick series.
    Wire `useMarketData` into `Chart.tsx` so the chart updates with new OHLCV data.
    Implement `HealthStatus.tsx` displaying a red indicator and warning overlay if `isHealthy` is false.
    Update `Dashboard.tsx` to include `HealthStatus` and distinctly show `currentPrice`.
  </action>
  <verify>
    <automated>npm run build --prefix web</automated>
  </verify>
  <done>Chart rendered, live price displayed, health indicated</done>
  <acceptance_criteria>web/src/components/Chart.tsx imports lightweight-charts and uses createChart</acceptance_criteria>
</task>

</tasks>

<success_criteria>
- Data polling hook functions
- Chart visualizes OHLCV data
- Health status overlay implemented
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-baseline-data-visualization/02-02-SUMMARY.md`
</output>
