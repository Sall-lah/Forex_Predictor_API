---
phase: 02-ui-baseline-data-visualization
plan: 04
type: execute
wave: 1
depends_on: ["03"]
files_modified:
  - web/src/index.css
  - web/src/components/Dashboard.tsx
  - web/src/components/HealthStatus.tsx
autonomous: false
requirements: ["UI-01", "UI-02", "DASH-05"]
gap_closure: true
must_haves:
  truths:
    - "User sees the Stitch-like dark themed dashboard styling instead of bare HTML."
    - "When backend polling fails, the dashboard shows a clear disconnected state and stale-data warning overlay (per D-03)."
    - "When polling succeeds again, the disconnected warning is removed and LIVE status is restored."
  artifacts:
    - path: "web/src/index.css"
      provides: "Design token utilities used by the Stitch-exported class names."
      contains: "@layer utilities"
    - path: "web/src/components/Dashboard.tsx"
      provides: "Dashboard-level stale-data overlay tied to market health state."
      contains: "!isHealthy"
    - path: "web/src/components/HealthStatus.tsx"
      provides: "Connectivity pill states for healthy/unhealthy status."
      contains: "DISCONNECTED"
  key_links:
    - from: "web/src/hooks/useMarketData.ts"
      to: "web/src/components/Dashboard.tsx"
      via: "isHealthy return value"
      pattern: "const \{ data, isHealthy, currentPrice \} = useMarketData\(\)"
    - from: "web/src/components/Dashboard.tsx"
      to: "web/src/components/HealthStatus.tsx"
      via: "isHealthy prop"
      pattern: "<HealthStatus isHealthy=\{isHealthy\}"
---

<objective>
Close the remaining UI gap from verification by ensuring Stitch-exported utility classes resolve to real styles and by rendering a visible stale-data warning when connectivity fails.

Purpose: Human verification reported bare HTML; this plan makes class-to-style mapping explicit and enforces D-03 degraded-state UX behavior.
Output: Stable design token utilities in global CSS, disconnected overlay behavior in Dashboard, and final visual verification instructions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-baseline-data-visualization/02-CONTEXT.md
@.planning/phases/02-ui-baseline-data-visualization/02-VERIFICATION.md

<interfaces>
From web/src/hooks/useMarketData.ts:
```ts
export const useMarketData = () => {
  return { data, error, isHealthy, currentPrice };
};
```

From web/src/components/HealthStatus.tsx:
```tsx
interface HealthStatusProps {
  isHealthy: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing dashboard design-token utilities</name>
  <files>web/src/index.css</files>
  <action>
    Extend `web/src/index.css` with a `@layer utilities` block that defines the custom classes used by the Stitch-exported markup (e.g., `bg-surface-container`, `bg-surface-container-high`, `bg-surface-container-low`, `bg-surface-container-highest`, `text-on-background`, `text-primary`, `text-outline`, `text-tertiary`, and related color aliases).
    Keep existing Tailwind directives and base layer intact.
    Ensure classes referenced in `Dashboard.tsx` resolve to actual CSS so the page does not render as unstyled bare HTML (addresses verification gap, per D-01/D-02 template usage context).
  </action>
  <verify>
    <automated>npm run build --prefix web</automated>
  </verify>
  <done>All major custom utility classes used in Dashboard markup have concrete style definitions and web build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement visible stale-data degraded mode per D-03</name>
  <files>web/src/components/Dashboard.tsx, web/src/components/HealthStatus.tsx</files>
  <action>
    Update `Dashboard.tsx` to show a non-intrusive stale-data warning overlay in the chart region when `isHealthy` is false (per locked decision D-03), while keeping the chart visible.
    Keep `HealthStatus` red/disconnected state behavior and ensure the copy clearly communicates reconnection attempt.
    Do not remove existing LIVE state behavior; only add degraded-state UX and keep component props contract unchanged.
  </action>
  <verify>
    <automated>npm run build --prefix web</automated>
  </verify>
  <done>Disconnected state produces both header indicator and chart-area stale-data warning overlay; healthy state removes the overlay.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final visual + interaction confirmation</name>
  <action>
    Run the frontend locally and guide the user through visual verification for both healthy and degraded connectivity states.
  </action>
  <verify>
    <automated>npm run dev --prefix web</automated>
    <what-built>Resolved class-driven styling and D-03 stale-data degraded mode UI.</what-built>
    <how-to-verify>
      1) Open the web app in browser.
      2) Confirm dark themed layout, card surfaces, typography, spacing, and icons match the Stitch baseline (not bare HTML).
      3) Simulate backend unavailability (stop API process) and verify `DISCONNECTED` pill appears with stale-data warning overlay while chart remains visible.
      4) Restore backend and verify status returns to `LIVE` and overlay disappears.
    </how-to-verify>
    <resume-signal>Type "approved" or provide exact mismatch details (component + expected vs actual).</resume-signal>
  </verify>
  <done>Human confirms visual parity and degraded-state behavior.</done>
</task>

</tasks>

<verification>
- `npm run build --prefix web` passes after CSS utility additions and degraded-state wiring.
- Human confirms styling and disconnect/recovery behavior in browser.
</verification>

<success_criteria>
- Dashboard is styled (no bare-HTML rendering).
- D-03 behavior is visible: disconnected indicator + stale-data overlay with chart still visible.
- Recovery path works: LIVE state restored when backend is healthy again.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-baseline-data-visualization/02-04-SUMMARY.md`
</output>
