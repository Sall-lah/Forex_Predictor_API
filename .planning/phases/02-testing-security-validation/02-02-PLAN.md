---
phase: 02-testing-security-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/middleware/rate_limit/test_performance.py
  - pytest.ini
autonomous: true
requirements:
  - TEST-04
  - TEST-06
  - TEST-07
must_haves:
  truths:
    - "Developer can run automated concurrency tests showing no race-condition over-allow under parallel request bursts."
    - "Developer can run automated load tests demonstrating rate-limit logic remains accurate under high-throughput synthetic traffic."
    - "Developer can run automated memory-stability tests (smoke + optional long-run) showing no unbounded state growth."
  artifacts:
    - path: "tests/middleware/rate_limit/test_performance.py"
      provides: "Concurrency, throughput, and memory-leak validation harness"
      min_lines: 180
    - path: "pytest.ini"
      provides: "Markers/configuration for controlled execution of heavy validation tests"
      contains: "ratelimit_perf"
  key_links:
    - from: "tests/middleware/rate_limit/test_performance.py"
      to: "app/middleware/rate_limit/service.py"
      via: "parallel evaluate/request workflows and assertion on allowed counts"
      pattern: "asyncio\.gather|allowed|429"
    - from: "tests/middleware/rate_limit/test_performance.py"
      to: "app/middleware/rate_limit/storage.py"
      via: "memory growth checks under rotating client keys"
      pattern: "max_entries|cleanup|memory|tracemalloc"
---

<objective>
Add rigorous concurrency, performance, and memory-stability validation so the rate limiter’s operational reliability is proven beyond functional correctness.

Purpose: Satisfy TEST-04/06/07 with repeatable automated stress checks that are practical for local and CI use.
Output: Dedicated performance/security test module with controlled heavy-test execution modes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-implementation/01-core-implementation-02-SUMMARY.md
@.planning/phases/01-core-implementation/01-core-implementation-03-SUMMARY.md
@.planning/phases/02-testing-security-validation/02-RESEARCH.md
@app/middleware/rate_limit/service.py
@app/middleware/rate_limit/storage.py
@tests/middleware/rate_limit/test_storage.py
</context>

<interfaces>
From `app/middleware/rate_limit/service.py`:
```python
class RateLimiterService:
    async def evaluate(self, request: Request) -> RateLimitServiceResult: ...
```

From `app/middleware/rate_limit/storage.py`:
```python
class InMemoryRateLimitStorage:
    async def get_state(self, key: str) -> RateLimitState | None: ...
    async def upsert_state(self, key: str, state: RateLimitState) -> None: ...
    async def cleanup_expired(self, now_monotonic: float) -> int: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build race-condition and throughput validation tests</name>
  <files>tests/middleware/rate_limit/test_performance.py, pytest.ini</files>
  <behavior>
    - Test 1: N concurrent requests against same client/endpoint never allow more than configured capacity in a burst window.
    - Test 2: High-volume request batches keep allow/deny accounting internally consistent.
    - Test 3: Throughput benchmark smoke test completes within defined time budget and reports deterministic assertions.
  </behavior>
  <action>Create `test_performance.py` containing async concurrency tests using `asyncio.gather` and FastAPI app/service fixtures tuned for deterministic burst checks (TEST-04). Add a high-throughput synthetic benchmark path for TEST-06 that verifies correctness first (no over-allow) and captures elapsed runtime for observability. Update `pytest.ini` with dedicated markers (e.g., `ratelimit_perf`) so heavy tests can be selected or skipped cleanly.</action>
  <verify>
    <automated>pytest tests/middleware/rate_limit/test_performance.py -k "concurrent or throughput" -x</automated>
  </verify>
  <done>Concurrency and throughput validations pass and reliably fail if race conditions or accounting drift are introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add memory-stability validation with smoke + soak modes</name>
  <files>tests/middleware/rate_limit/test_performance.py, pytest.ini</files>
  <behavior>
    - Test 1: Rotating-client workload with periodic cleanup keeps memory/state bounded in smoke duration.
    - Test 2: Optional long-run mode (env/marker gated) executes extended soak validation for 1h profile.
  </behavior>
  <action>Extend `test_performance.py` with memory-growth checks using `tracemalloc` and rotating client identities while invoking storage cleanup logic to validate TEST-07. Implement dual-mode execution: short default smoke scenario for routine runs plus explicit long-run scenario enabled via marker or env var (e.g., `RATE_LIMIT_SOAK=1`) for full 1-hour validation. Document invocation through test docstrings and marker naming in `pytest.ini`.</action>
  <verify>
    <automated>pytest tests/middleware/rate_limit/test_performance.py -k "memory or soak" -x</automated>
  </verify>
  <done>Memory tests show bounded growth in smoke mode and provide a reproducible command path for full soak verification.</done>
</task>

</tasks>

<verification>
Run:
- `pytest tests/middleware/rate_limit/test_performance.py -k "concurrent or throughput or memory" -x`
- Optional full soak: `RATE_LIMIT_SOAK=1 pytest tests/middleware/rate_limit/test_performance.py -k "soak" -x`
</verification>

<success_criteria>
- TEST-04, TEST-06, TEST-07 each have explicit automated validation paths.
- Heavy tests are controllable via markers/env switches and do not block normal fast test loops.
- Performance assertions prioritize correctness (no race-condition over-allow) with measurable runtime/memory evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-testing-security-validation/02-testing-security-validation-02-SUMMARY.md`
</output>
