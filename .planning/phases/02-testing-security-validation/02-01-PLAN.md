---
phase: 02-testing-security-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/middleware/rate_limit/service.py
  - tests/middleware/rate_limit/test_bucket.py
  - tests/middleware/rate_limit/test_middleware.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-05
must_haves:
  truths:
    - "Developer can run focused tests proving within-limit requests pass and over-limit requests return 429 with correct headers."
    - "Developer can run tests proving token refill behavior restores request capacity over elapsed time."
    - "Developer can run security tests proving untrusted X-Forwarded-For values cannot bypass per-IP enforcement."
  artifacts:
    - path: "tests/middleware/rate_limit/test_middleware.py"
      provides: "Happy path + 429 + spoofing resistance test coverage"
      min_lines: 180
    - path: "tests/middleware/rate_limit/test_bucket.py"
      provides: "Deterministic token refill and edge-case bucket validation"
      min_lines: 120
    - path: "app/middleware/rate_limit/service.py"
      provides: "Trusted-proxy-safe IP resolution and strict exemption matching"
      contains: "_resolve_client_ip"
  key_links:
    - from: "tests/middleware/rate_limit/test_middleware.py"
      to: "app/middleware/rate_limit/middleware.py"
      via: "FastAPI TestClient requests asserting response headers/body"
      pattern: "X-RateLimit-Limit|Retry-After|status_code == 429"
    - from: "tests/middleware/rate_limit/test_middleware.py"
      to: "app/middleware/rate_limit/service.py"
      via: "spoofing tests around trusted/untrusted proxy handling"
      pattern: "x-forwarded-for|trusted proxy|untrusted"
---

<objective>
Strengthen and validate core functional + security behavior of the rate limiter so Phase 1 implementation is provably correct under realistic attack and edge conditions.

Purpose: Satisfy TEST-01/02/03/05 with executable, deterministic tests and close any security logic gaps discovered.
Output: Hardened service behavior plus complete functional/security test suite for middleware and bucket logic.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-implementation/01-core-implementation-03-SUMMARY.md
@.planning/phases/02-testing-security-validation/02-RESEARCH.md
@app/middleware/rate_limit/service.py
@app/middleware/rate_limit/middleware.py
@tests/middleware/rate_limit/test_bucket.py
@tests/middleware/rate_limit/test_middleware.py
</context>

<interfaces>
From `app/middleware/rate_limit/service.py`:
```python
class RateLimiterService:
    async def evaluate(self, request: Request) -> RateLimitServiceResult: ...
    def _resolve_client_ip(self, request: Request) -> str: ...
```

From `app/middleware/rate_limit/middleware.py`:
```python
class RateLimitMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request: Request, call_next) -> Response: ...
```

From `app/middleware/rate_limit/schemas.py`:
```python
class RateLimitDecision(BaseModel):
    allowed: bool
    limit: int
    remaining: int
    reset_after_seconds: int
    retry_after_seconds: int | None
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden client-IP and exemption security logic</name>
  <files>app/middleware/rate_limit/service.py, tests/middleware/rate_limit/test_middleware.py</files>
  <behavior>
    - Test 1: Untrusted client with forged X-Forwarded-For remains limited by socket IP identity.
    - Test 2: Trusted proxy chain resolves effective client safely from proxy header chain.
    - Test 3: Exemptions apply only to exact configured operational paths, not traversal-like variants.
  </behavior>
  <action>Update `RateLimiterService` to enforce spoofing-resistant client identification (per Pitfall guidance in research): only honor `X-Forwarded-For` when direct socket IP is trusted, and resolve effective client from the trusted chain safely instead of naive first-value parsing. Normalize path comparison for exemption checks (strip query string + normalize trailing slash) while keeping exact allowlist semantics. Extend `test_middleware.py` with explicit spoofing and exemption-bypass cases (TEST-05) and keep existing behavior coverage green (TEST-01/TEST-02).</action>
  <verify>
    <automated>pytest tests/middleware/rate_limit/test_middleware.py -k "spoof or exempt or 429 or headers" -x</automated>
  </verify>
  <done>Security-focused tests fail before logic update, pass after update, and prove spoofing/traversal bypass attempts do not evade limits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand refill and boundary behavior tests</name>
  <files>tests/middleware/rate_limit/test_bucket.py, tests/middleware/rate_limit/test_middleware.py</files>
  <behavior>
    - Test 1: Requests within available token budget are allowed with decreasing remaining counts.
    - Test 2: Exhausted bucket returns deny decision with positive reset/retry timings.
    - Test 3: Elapsed time refill restores capacity continuously and allows subsequent request.
  </behavior>
  <action>Add deterministic tests for token refill cadence and boundary behavior to make TEST-03 explicit (use controllable clock injection in bucket tests, avoid flaky sleep-heavy assertions). Add middleware-level tests that validate header values remain consistent across allow→deny transitions for same client/endpoint (TEST-01, TEST-02). Keep tests isolated and fast (&lt;60s total).</action>
  <verify>
    <automated>pytest tests/middleware/rate_limit/test_bucket.py tests/middleware/rate_limit/test_middleware.py -x</automated>
  </verify>
  <done>Functional and refill behavior are proven by deterministic tests with stable pass/fail outcomes across repeated runs.</done>
</task>

</tasks>

<verification>
Run `pytest tests/middleware/rate_limit/test_bucket.py tests/middleware/rate_limit/test_middleware.py -x` and confirm all tests pass with no flaky timing assertions.
</verification>

<success_criteria>
- TEST-01, TEST-02, TEST-03, and TEST-05 have explicit automated coverage.
- Spoofing and exemption-bypass cases are represented as executable tests and passing.
- Service + middleware behavior remains contract-compliant for 200 and 429 responses.
</success_criteria>

<output>
After completion, create `.planning/phases/02-testing-security-validation/02-testing-security-validation-01-SUMMARY.md`
</output>
