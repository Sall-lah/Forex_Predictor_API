---
phase: 01-core-implementation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/config.py
  - app/middleware/rate_limit/storage.py
  - tests/middleware/rate_limit/test_storage.py
autonomous: true
requirements:
  - CORE-04
  - CONF-02
  - CONF-03
  - STOR-01
  - STOR-02
  - STOR-03
  - STRUC-03
  - STRUC-04
must_haves:
  truths:
    - "Operator can configure route-specific rate limits and storage safety knobs through environment variables."
    - "Rate-limit state persists in memory per key with thread-safe atomic updates."
    - "Expired/stale bucket entries are cleaned up to prevent unbounded memory growth."
  artifacts:
    - path: "app/core/config.py"
      provides: "Rate-limit settings and per-endpoint overrides"
    - path: "app/middleware/rate_limit/storage.py"
      provides: "In-memory async storage and cleanup operations"
    - path: "tests/middleware/rate_limit/test_storage.py"
      provides: "Verification for lock safety and TTL cleanup"
  key_links:
    - from: "app/core/config.py"
      to: "app/middleware/rate_limit/storage.py"
      via: "Storage initialization using configured limits and cleanup settings"
      pattern: "RATE_LIMIT|cleanup|max_entries"
---

<objective>
Provide configurable rate-limit settings and thread-safe in-memory storage with cleanup so middleware can enforce limits reliably under concurrent load.

Purpose: Establish durable state handling and operator controls before middleware wiring.
Output: Config extensions + tested storage backend.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-implementation/01-RESEARCH.md
@app/core/config.py
</context>

<interfaces>
From `app/core/config.py` current pattern:
```python
class Settings(BaseSettings):
    KRAKEN_TIMEOUT: float = 15.0
```

Contract to add:
```python
RATE_LIMIT_DEFAULT_CAPACITY: int
RATE_LIMIT_DEFAULT_REFILL_RATE_PER_SECOND: float
RATE_LIMIT_PREDICTION_CAPACITY: int
RATE_LIMIT_PREDICTION_REFILL_RATE_PER_SECOND: float
RATE_LIMIT_HISTORICAL_CAPACITY: int
RATE_LIMIT_HISTORICAL_REFILL_RATE_PER_SECOND: float
RATE_LIMIT_STORAGE_MAX_ENTRIES: int
RATE_LIMIT_STORAGE_TTL_SECONDS: int
RATE_LIMIT_TRUSTED_PROXY_IPS: str
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Settings with rate-limit environment configuration</name>
  <read_first>
    - app/core/config.py
    - .env
  </read_first>
  <files>app/core/config.py</files>
  <behavior>
    - Test 1: Settings exposes default and endpoint-specific limits.
    - Test 2: Settings exposes storage TTL/max entries and trusted proxy list string.
  </behavior>
  <action>Add CONF-02/CONF-03 settings to `Settings` using explicit typed fields: `RATE_LIMIT_DEFAULT_CAPACITY=60`, `RATE_LIMIT_DEFAULT_REFILL_RATE_PER_SECOND=1.0`, `RATE_LIMIT_PREDICTION_CAPACITY=10`, `RATE_LIMIT_PREDICTION_REFILL_RATE_PER_SECOND=10/60`, `RATE_LIMIT_HISTORICAL_CAPACITY=100`, `RATE_LIMIT_HISTORICAL_REFILL_RATE_PER_SECOND=100/60`, `RATE_LIMIT_STORAGE_MAX_ENTRIES=100000`, `RATE_LIMIT_STORAGE_TTL_SECONDS=3600`, `RATE_LIMIT_TRUSTED_PROXY_IPS=""`, and `RATE_LIMIT_EXEMPT_PATHS="/health,/docs,/redoc,/openapi.json"` with docstrings/comments consistent with existing style.</action>
  <verify>
    <automated>pytest tests/middleware/rate_limit/test_storage.py -k "config" -x</automated>
  </verify>
  <acceptance_criteria>
    - `config.py` contains all `RATE_LIMIT_` fields listed in action.
    - `RATE_LIMIT_EXEMPT_PATHS` default includes `/health,/docs,/redoc,/openapi.json`.
  </acceptance_criteria>
  <done>Operator-facing configuration values exist and are type-safe via pydantic-settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement async-safe in-memory storage with cleanup</name>
  <read_first>
    - app/middleware/rate_limit/schemas.py
    - app/core/config.py
  </read_first>
  <files>app/middleware/rate_limit/storage.py, tests/middleware/rate_limit/test_storage.py</files>
  <behavior>
    - Test 1: set/get/update for a key is atomic under concurrent async calls.
    - Test 2: cleanup removes entries exceeding TTL and keeps fresh entries.
    - Test 3: storage enforces max-entry guard behavior.
  </behavior>
  <action>Implement `InMemoryRateLimitStorage` for STOR-01/STOR-02/STOR-03 using `dict[str, RateLimitState]` + `asyncio.Lock`. Provide async methods `get_state`, `upsert_state`, `delete_state`, and `cleanup_expired(now_monotonic: float)` that remove stale entries by `last_refill_at`. Add optional simple FIFO/LRU-safe eviction when `RATE_LIMIT_STORAGE_MAX_ENTRIES` is reached, and write tests proving atomic correctness and cleanup behavior.</action>
  <verify>
    <automated>pytest tests/middleware/rate_limit/test_storage.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `storage.py` contains `class InMemoryRateLimitStorage`.
    - `storage.py` contains `asyncio.Lock`.
    - `test_storage.py` includes concurrency and cleanup tests and exits 0.
  </acceptance_criteria>
  <done>Storage layer is thread-safe, bounded, and ready for middleware integration.</done>
</task>

</tasks>

<verification>
Run `pytest tests/middleware/rate_limit/test_storage.py -x` and confirm pass.
</verification>

<success_criteria>
- Config includes all required rate-limit env vars with sane defaults.
- Storage supports atomic operations and TTL cleanup with passing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-implementation/01-core-implementation-02-SUMMARY.md`
</output>
