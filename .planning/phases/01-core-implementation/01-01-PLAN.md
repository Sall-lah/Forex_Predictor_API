---
phase: 01-core-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/middleware/rate_limit/__init__.py
  - app/middleware/rate_limit/schemas.py
  - app/middleware/rate_limit/bucket.py
  - tests/middleware/rate_limit/test_bucket.py
autonomous: true
requirements:
  - CORE-01
  - STRUC-01
  - STRUC-02
  - STRUC-03
  - STRUC-04
must_haves:
  truths:
    - "Rate limiter package exists under app/middleware/rate_limit and can be imported by later plans."
    - "Token bucket behavior refills continuously over elapsed time and denies when token balance is insufficient."
    - "Bucket behavior is test-proven for consume, deny, and refill edge cases."
  artifacts:
    - path: "app/middleware/rate_limit/schemas.py"
      provides: "Typed Pydantic models for rate-limit configuration and decision payloads"
    - path: "app/middleware/rate_limit/bucket.py"
      provides: "TokenBucket class with deterministic consume/refill logic"
    - path: "tests/middleware/rate_limit/test_bucket.py"
      provides: "Executable proof for token-bucket correctness"
  key_links:
    - from: "app/middleware/rate_limit/bucket.py"
      to: "app/middleware/rate_limit/schemas.py"
      via: "Token bucket methods consuming typed config/state models"
      pattern: "from app\.middleware\.rate_limit\.schemas import"
---

<objective>
Create the foundational rate-limit contracts and token-bucket engine so downstream storage/service/middleware work can build against stable interfaces.

Purpose: De-risk the hardest math and contract decisions first.
Output: Importable `app/middleware/rate_limit/` package with tested bucket behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-implementation/01-RESEARCH.md
@app/core/config.py
</context>

<interfaces>
From `app/core/config.py`:
```python
class Settings(BaseSettings):
    API_PREFIX: str = "/api/v1"

@lru_cache
def get_settings() -> Settings: ...
```

Contract to create in this plan:
```python
class RateLimitPolicy(BaseModel):
    capacity: int
    refill_rate_per_second: float

class RateLimitDecision(BaseModel):
    allowed: bool
    limit: int
    remaining: int
    reset_after_seconds: int
    retry_after_seconds: int | None
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create rate-limit package and typed contracts</name>
  <read_first>
    - app/core/config.py
    - app/main.py
  </read_first>
  <files>app/middleware/rate_limit/__init__.py, app/middleware/rate_limit/schemas.py</files>
  <behavior>
    - Test 1: Contract models validate positive capacities and refill rates.
    - Test 2: Decision model can represent both allowed and denied outcomes.
  </behavior>
  <action>Create `app/middleware/rate_limit/` (STRUC-01) with `__init__.py` and `schemas.py`. In `schemas.py`, define Pydantic models `RateLimitPolicy`, `RateLimitState`, and `RateLimitDecision` with full type hints (STRUC-03), explicit validation (`capacity > 0`, `refill_rate_per_second > 0`), and docstrings. Export these in `__init__.py` for modular reuse (STRUC-02, STRUC-04).</action>
  <verify>
    <automated>pytest tests/middleware/rate_limit/test_bucket.py -k "schema" -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/middleware/rate_limit/schemas.py` contains `class RateLimitPolicy`.
    - `app/middleware/rate_limit/schemas.py` contains `class RateLimitDecision`.
    - `app/middleware/rate_limit/__init__.py` exports schema classes.
  </acceptance_criteria>
  <done>Typed contracts are importable and enforce valid policy values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TokenBucket with continuous refill math</name>
  <read_first>
    - app/middleware/rate_limit/schemas.py
    - .planning/phases/01-core-implementation/01-RESEARCH.md
  </read_first>
  <files>app/middleware/rate_limit/bucket.py, tests/middleware/rate_limit/test_bucket.py</files>
  <behavior>
    - Test 1: Consuming when tokens are available returns allowed with decremented remaining.
    - Test 2: Consuming when tokens are empty returns denied with positive retry seconds.
    - Test 3: Elapsed time refills tokens continuously up to configured capacity.
  </behavior>
  <action>Implement `TokenBucket` in `bucket.py` for CORE-01 using monotonic time (`time.monotonic()`), refill formula `new_tokens = min(capacity, tokens + elapsed * refill_rate_per_second)`, and deterministic decision creation. Return `RateLimitDecision` for both allow/deny paths with rounded-up reset/retry seconds (`math.ceil`) so headers are stable across middleware responses.</action>
  <verify>
    <automated>pytest tests/middleware/rate_limit/test_bucket.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `bucket.py` contains `class TokenBucket`.
    - `bucket.py` contains `time.monotonic` usage.
    - `test_bucket.py` includes allow, deny, and refill tests and exits 0.
  </acceptance_criteria>
  <done>Bucket logic is production-safe and test-proven for core token behavior.</done>
</task>

</tasks>

<verification>
Run `pytest tests/middleware/rate_limit/test_bucket.py -x` and confirm all tests pass.
</verification>

<success_criteria>
- Token-bucket contracts and implementation are present in dedicated middleware directory.
- Bucket behavior is verified by automated tests for consume/refill/deny scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-implementation/01-core-implementation-01-SUMMARY.md`
</output>
