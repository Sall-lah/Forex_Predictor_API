---
phase: 01-core-implementation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app/middleware/rate_limit/service.py
  - app/middleware/rate_limit/middleware.py
  - app/main.py
  - tests/middleware/rate_limit/test_middleware.py
autonomous: true
requirements:
  - CORE-02
  - CORE-03
  - CORE-05
  - HTTP-01
  - HTTP-02
  - HTTP-03
  - HTTP-04
  - HTTP-05
  - CONF-01
  - CONF-04
  - STRUC-02
  - STRUC-04
  - STRUC-05
must_haves:
  truths:
    - "Client exceeding endpoint quota receives HTTP 429 with retry guidance and consistent rate-limit headers."
    - "Operational endpoints (/health, /docs, /redoc, /openapi.json) are exempt and never rate limited."
    - "Clients behind trusted proxy are limited by real client IP, while untrusted forwarded headers are ignored."
    - "Different endpoint families can enforce different limits from environment configuration."
  artifacts:
    - path: "app/middleware/rate_limit/service.py"
      provides: "Client IP resolution, endpoint policy resolution, and decision orchestration"
    - path: "app/middleware/rate_limit/middleware.py"
      provides: "Request interception, header injection, and 429 responses"
    - path: "app/main.py"
      provides: "Middleware registration in FastAPI app lifecycle"
    - path: "tests/middleware/rate_limit/test_middleware.py"
      provides: "End-to-end behavior verification for middleware contract"
  key_links:
    - from: "app/main.py"
      to: "app/middleware/rate_limit/middleware.py"
      via: "app.add_middleware(...)"
      pattern: "add_middleware\(.*RateLimitMiddleware"
    - from: "app/middleware/rate_limit/middleware.py"
      to: "app/middleware/rate_limit/service.py"
      via: "per-request enforcement call"
      pattern: "RateLimiterService"
    - from: "app/middleware/rate_limit/service.py"
      to: "app/middleware/rate_limit/storage.py"
      via: "atomic state fetch/update"
      pattern: "InMemoryRateLimitStorage"
---

<objective>
Integrate the rate limiter into FastAPI request flow with secure client identification, exemption handling, endpoint-specific policies, and standards-compliant 429/header responses.

Purpose: Deliver the user-visible Phase 1 outcome (working protection on live API endpoints).
Output: Fully wired middleware with verification tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-implementation/01-RESEARCH.md
@app/main.py
@app/core/exceptions.py
@app/api/router.py
</context>

<interfaces>
From `app/core/exceptions.py`:
```python
class BaseAppException(Exception): ...
```

From plan dependencies:
```python
class RateLimitDecision(BaseModel):
    allowed: bool
    limit: int
    remaining: int
    reset_after_seconds: int
    retry_after_seconds: int | None
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement RateLimiter service orchestration and secure client identification</name>
  <read_first>
    - app/middleware/rate_limit/schemas.py
    - app/middleware/rate_limit/bucket.py
    - app/middleware/rate_limit/storage.py
    - app/core/config.py
  </read_first>
  <files>app/middleware/rate_limit/service.py, tests/middleware/rate_limit/test_middleware.py</files>
  <behavior>
    - Test 1: Service uses endpoint-specific limits for prediction and historical routes.
    - Test 2: Service extracts client IP from X-Forwarded-For only when source IP is trusted.
    - Test 3: Service bypasses exempt paths and returns no-limit decision.
  </behavior>
  <action>Create `RateLimiterService` that composes `TokenBucket` + `InMemoryRateLimitStorage`. Implement methods to: (a) parse trusted proxies from `RATE_LIMIT_TRUSTED_PROXY_IPS`, (b) resolve client IP with anti-spoofing rules for CORE-02, (c) map request paths to policy (`/api/v1/prediction` uses prediction policy, `/api/v1/historic-data` uses historical policy, fallback default), and (d) short-circuit exempt paths from `RATE_LIMIT_EXEMPT_PATHS` (CONF-04). Ensure return object always contains exact header values for middleware use.</action>
  <verify>
    <automated>pytest tests/middleware/rate_limit/test_middleware.py -k "service or proxy or exempt or override" -x</automated>
  </verify>
  <acceptance_criteria>
    - `service.py` contains `class RateLimiterService`.
    - `service.py` contains trusted proxy parsing and `x-forwarded-for` logic.
    - `service.py` contains endpoint policy branches for `/prediction` and `/historic-data`.
  </acceptance_criteria>
  <done>Service layer produces secure and policy-aware rate-limit decisions for middleware.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire middleware into FastAPI with headers and 429 responses</name>
  <read_first>
    - app/main.py
    - app/api/router.py
    - app/middleware/rate_limit/service.py
  </read_first>
  <files>app/middleware/rate_limit/middleware.py, app/main.py, tests/middleware/rate_limit/test_middleware.py</files>
  <behavior>
    - Test 1: Requests over limit return 429 with HTTP-01..HTTP-04 headers and JSON body containing reason/retry seconds.
    - Test 2: Allowed requests include HTTP-01..HTTP-03 headers with decreasing remaining quota.
    - Test 3: Exempt endpoints never trigger 429 regardless of request volume.
  </behavior>
  <action>Implement `RateLimitMiddleware` (CONF-01) as FastAPI/Starlette HTTP middleware that calls `RateLimiterService` per request, injects `X-RateLimit-Limit`, `X-RateLimit-Remaining`, and `X-RateLimit-Reset` on controlled responses (HTTP-01/02/03), and emits 429 JSON body with explicit message + `retry_after_seconds` and `Retry-After` header (CORE-05, HTTP-04, HTTP-05). Register middleware in `app/main.py` via `app.add_middleware(RateLimitMiddleware)` so protection applies globally to API routes.</action>
  <verify>
    <automated>pytest tests/middleware/rate_limit/test_middleware.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `middleware.py` contains `class RateLimitMiddleware`.
    - `main.py` contains `app.add_middleware(RateLimitMiddleware`.
    - Middleware tests verify 429 + all required headers and exit 0.
  </acceptance_criteria>
  <done>Global middleware enforcement is active with compliant headers, exemptions, and error responses.</done>
</task>

</tasks>

<verification>
Run `pytest tests/middleware/rate_limit/test_middleware.py -x` then `pytest -x` to ensure no regression.
</verification>

<success_criteria>
- Middleware enforces per-IP and per-endpoint limits with trusted-proxy handling.
- 429 responses and limit headers match roadmap success criteria.
- Exempt operational endpoints remain unaffected by rate limits.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-implementation/01-core-implementation-03-SUMMARY.md`
</output>
