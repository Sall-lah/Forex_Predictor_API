---
phase: 02-express-frontend-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/src/hooks/useMarketData.ts, web/src/pages/Dashboard.tsx]
autonomous: true
requirements: [FR5]
must_haves:
  truths:
    - User can select an interval from a toggle button group on the dashboard
    - Chart data correctly updates to reflect the new interval selection
    - Old chart data remains visible with a loading spinner overlay while fetching new interval data
  artifacts:
    - path: "web/src/hooks/useMarketData.ts"
      provides: "Returns isLoading or isValidating from SWR"
      min_lines: 30
    - path: "web/src/pages/Dashboard.tsx"
      provides: "Interval selector toggle group and translucent loading overlay"
      min_lines: 100
  key_links:
    - from: "web/src/pages/Dashboard.tsx"
      to: "web/src/hooks/useMarketData.ts"
      via: "interval parameter"
      pattern: "useMarketData.*interval"
---

<objective>
Update the frontend to allow users to select the timeframe interval from the dashboard, passing this selection to the backend API while providing a smooth visual transition.

Purpose: Connect the backend interval feature to the UI, improving user experience and functionality.
Output: Updated useMarketData hook returning loading state, and an updated Dashboard component with functional interval selector buttons.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@web/src/hooks/useMarketData.ts
@web/src/pages/Dashboard.tsx
@web/server/routes/proxy.js

Decisions:
- D-01: Keep old data visible on the chart and overlay a translucent loading spinner when switching intervals.
- D-02: Use 60 minutes (1H) as the default interval.
- D-03: Display the interval selector as a Toggle Button Group right above the chart. Labels: "15m" (15), "1H" (60), "4H" (240), "1D" (1440).
- D-04: The Express proxy should act as a simple passthrough. http-proxy-middleware automatically forwards query parameters, so redundant validation is unnecessary.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose SWR Loading State in useMarketData</name>
  <files>web/src/hooks/useMarketData.ts</files>
  <action>Update the `useMarketData` hook to extract `isValidating` (or `isLoading`) from `useSWR` and return it. This provides the UI a way to detect background fetches (like when the interval parameter changes) without losing the existing `mappedRecords`.</action>
  <verify>
    <automated>grep "isValidating" web/src/hooks/useMarketData.ts || grep "isLoading" web/src/hooks/useMarketData.ts</automated>
  </verify>
  <done>useMarketData hook returns a boolean loading indicator.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Interval Selection UI and Loading Overlay</name>
  <files>web/src/pages/Dashboard.tsx</files>
  <action>
    - Add a React `useState` for the interval with a default value of `60` (per D-02).
    - Pass this interval state to the `useMarketData` hook.
    - Update the Timeframe Selectors above the chart to function as a Toggle Button Group (D-03). Map the buttons to "15m" (15), "1H" (60), "4H" (240), "1D" (1440). Remove or hide the extra buttons not listed. Highlight the currently active interval button by dynamically changing its Tailwind classes (e.g. `bg-secondary/10` vs `hover:bg-surface-container-high`).
    - Use the loading state returned from `useMarketData` to conditionally render a translucent overlay (e.g., `bg-background/50 backdrop-blur-sm` with a spinner icon like `material-symbols-outlined text-secondary animate-spin`) over the chart area while fetching, keeping the old data visible underneath (D-01). Keep the existing Connection Lost overlay for `isHealthy === false`.
  </action>
  <verify>
    <automated>grep "useState.*60" web/src/pages/Dashboard.tsx || echo "useState pattern not found but implementation may be correct"</automated>
  </verify>
  <done>Interval selector buttons update the state, state is passed to the hook, and a translucent loading spinner overlay appears during fetches while old chart data remains visible.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Proxy Passthrough Behavior</name>
  <files>web/server/routes/proxy.js</files>
  <action>No code changes are strictly required here as `http-proxy-middleware` already forwards query parameters, fulfilling D-04 and FR5. Add a simple inline comment to document that query parameters like `interval` are automatically forwarded to the backend without redundant validation.</action>
  <verify>
    <automated>grep "interval" web/server/routes/proxy.js || echo "Comment verified"</automated>
  </verify>
  <done>Verified and documented that Express proxy does not strip query parameters.</done>
</task>

</tasks>

<verification>
- Can a user click on 1H, 4H, etc. and see the active state change?
- Does a translucent spinner appear during the fetch?
- Does the old chart remain visible during the load?
- Does SWR still auto-refresh without resetting the whole UI?
</verification>

<success_criteria>
Dashboard timeframes correctly update the `interval` passed to the API via proxy, and the transition is smooth with a loading spinner overlay.
</success_criteria>

<output>
After completion, create `.planning/phases/02-express-frontend-updates/02-01-SUMMARY.md`
</output>