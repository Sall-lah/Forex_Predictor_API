# Milestone Archive Template

---

## File Template

# Milestone v1.0: Timeframe Interval Support

**Status:** ✅ SHIPPED 2026-04-19
**Phases:** 1-2
**Total Plans:** 2

## Overview

Added dynamic timeframe interval support to Kraken OHLCV fetches across the API stack.
Integrated interval selection toggle buttons on the React Dashboard with SWR-driven background loading overlay.

## Phases

### Phase 1: API Updates

**Goal**: Update API schemas, client, services, and routers for interval
**Depends on**: none
**Plans**: 1 plans

Plans:

- [x] 01-01: Update API schemas, client, services, and routers for interval

**Details:**
- Modified `PredictionRequest` to strictly validate `interval` using Pydantic's `Literal` type.
- Updated `KrakenAPIClient.fetch_ohlcv_data` and query building to accept and forward the interval parameter.

### Phase 2: Express / Frontend Updates

**Goal**: Update the Express proxy and React frontend to support UI selection of the timeframe interval.
**Depends on**: Phase 1
**Plans**: 1 plans

Plans:

- [x] 02-01: Integrated interval selection toggle buttons on the React Dashboard with SWR-driven background loading overlay

**Details:**
- Used isValidating from SWR to show loading overlay without clearing existing chart data
- Configured UI to default to 60m (1H) interval

---

## Milestone Summary

**Key Decisions:**
- Validated interval parameters strictly against allowed Kraken timeframes using Literal/Enum
- Preserved default 60-minute interval backward compatibility
- Used isValidating from SWR to show loading overlay without clearing existing chart data
- Configured UI to default to 60m (1H) interval

**Issues Resolved:**
- Hardcoded timeframe limitation

**Issues Deferred:**
- None

**Technical Debt Incurred:**
- Quick Tasks deferred: 260419-erg-refactor-react-so-it-does-not-use-any-us, 260419-g0k-refactor-the-react-component-to-use-hook

---

_For current project status, see .planning/ROADMAP.md_