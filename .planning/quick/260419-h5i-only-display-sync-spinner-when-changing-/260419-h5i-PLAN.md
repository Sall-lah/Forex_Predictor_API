---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/src/hooks/useMarketData.ts, web/src/pages/Dashboard.tsx]
autonomous: true
requirements: []
must_haves:
  truths:
    - Sync spinner does not blink during routine 15s polling background updates
    - Sync spinner appears on initial load and when changing intervals
  artifacts:
    - path: web/src/hooks/useMarketData.ts
      provides: Exports isLoading from useSWR
    - path: web/src/pages/Dashboard.tsx
      provides: Checks isLoading instead of isValidating for the spinner
  key_links: []
---

<objective>
Only display the sync spinner on initial load and when changing intervals, avoiding the annoying blinking effect during SWR background polling revalidation.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Use isLoading instead of isValidating for sync spinner</name>
  <files>web/src/hooks/useMarketData.ts, web/src/pages/Dashboard.tsx</files>
  <action>
    - In `web/src/hooks/useMarketData.ts`, extract `isLoading` from `useSWR` and add it to the returned object.
    - In `web/src/pages/Dashboard.tsx`, destructure `isLoading` from `useMarketData` and change the condition from `{isValidating && isHealthy && ...}` to `{isLoading && isHealthy && ...}` so that the spinner only shows when there is no data in cache yet.
  </action>
  <verify>
    <automated>npm --prefix web run build</automated>
  </verify>
  <done>The sync spinner condition correctly checks `isLoading`.</done>
</task>
</tasks>
