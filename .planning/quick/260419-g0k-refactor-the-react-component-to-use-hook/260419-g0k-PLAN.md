---
phase: quick
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: ["web/src/components/Chart.tsx"]
autonomous: true
requirements: ["REFACTOR-1"]
must_haves:
  truths:
    - "Chart.tsx successfully uses hooks to track side-effects"
    - "No lightweight-charts imperative updates happen directly during the React render phase"
  artifacts:
    - path: "web/src/components/Chart.tsx"
      provides: "Proper React component using hooks"
  key_links: []
---

<objective>
Refactor `Chart.tsx` to properly use the `useEffect` hook for data synchronization side-effects, removing imperative updates from the main render body.

Purpose: Side-effects (like modifying ref values and invoking `applyDataToSeries`) should not happen directly during the React render cycle, which can cause erratic behavior and violates React strict mode principles.
Output: A cleaner, warning-free React functional component that effectively leverages hooks for lifecycle and prop changes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@web/src/components/Chart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor data update logic to use useEffect</name>
  <files>web/src/components/Chart.tsx</files>
  <action>
    Modify `web/src/components/Chart.tsx` to replace the direct render phase data sync with a `useEffect` hook:
    1. Import `useEffect` from `react` (along with `useRef` and `useCallback`).
    2. Move the conditional block that checks `data !== prevDataRef.current` and calls `applyDataToSeries(data, seriesRef.current)` into a `useEffect` hook that lists `[data]` in its dependency array.
    3. Remove any side-effects that execute unconditionally outside of the hook callbacks.
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <done>The data update condition is encapsulated in a `useEffect` hook and the component builds cleanly.</done>
</task>

</tasks>

<verification>
Ensure the project builds without TypeScript or Vite errors. The code in `Chart.tsx` should only have side effects executing within hook callbacks or event handlers.
</verification>

<success_criteria>
- The functional component correctly leverages hooks instead of violating strict-mode.
- No direct render-phase mutations of refs or lightweight-chart state occur.
- Application bundles successfully with `npm run build`.
</success_criteria>

<output>
After completion, create `.planning/quick/260419-g0k-refactor-the-react-component-to-use-hook/quick-1-SUMMARY.md`
</output>
